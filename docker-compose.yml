name: mkvlrn-ormacarbon-db
services:
  postgres:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    volumes:
      - mkvlrn-ormacarbon-data:/var/lib/postgresql/data

volumes:
  mkvlrn-ormacarbon-data:
