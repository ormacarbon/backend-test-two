{
  "name": "api",
  "version": "1.0.0",
  "description": "api project that references the loss of data from the beer company",
  "main": "index.js",
  "scripts": {
    "init": "npm i && npm i -g nodemon && nodemon index.js",
    "start_bd": "npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "gitconfig": "git config --local user.name \"Maria Fernanda Oliveira\" && git config --local user.email \"mariafernanda.oliveira281@gmail.com\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maria Fernanda",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^3.1.2",
    "path": "^0.12.7",
    "sequelize": "^6.28.0",
    "sequelize-cli": "^6.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
